
# Failed Login Detection
index=security_logs "FAILED LOGIN"

# Brute Force Detection
index=security_logs "FAILED LOGIN"
| stats count by ip
| where count > 5

# Suspicious IP Activity
index=security_logs
| stats count by ip
| sort - count
